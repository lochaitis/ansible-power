---
- name: "backupios demo"
  hosts: all
  gather_facts: no

  user: devops

  collections:
  - ibm.power_vios
  tasks:

  - name: Create an mksysb of the system
    backupios:
      file: /home/padmin/backup.mksysb
      mksysb: yes
      savemedialib: no
